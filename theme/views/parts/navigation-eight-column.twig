<ul class="nav-main">
  {% set count_eight = 1 %}
  {% for item in eight_column.items %}

    <li
      class="header-dropdown-item nav-main-item {{ item.classes|join(' ') }}">

      <a data-current="{{ eight_column.title|replace({' ': ''})|lower }}" data-open="{{ item.slug }}"
         class="nav-main-link"
         href="{{ item.link }}" {% if item._menu_item_target %}target="{{ item._menu_item_target }}"{% endif %}>{{ item.title }}</a>

    </li>
    {% set count_eight = count_eight + 1 %}
  {% endfor %}
</ul>
